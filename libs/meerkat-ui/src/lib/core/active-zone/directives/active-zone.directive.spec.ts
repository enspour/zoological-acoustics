import { MkActiveZoneDirective } from './active-zone.directive';

describe('MkActiveZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new MkActiveZoneDirective();
    expect(directive).toBeTruthy();
  });
});
