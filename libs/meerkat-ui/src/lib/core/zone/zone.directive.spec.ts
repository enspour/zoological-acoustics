import { MkZoneDirective } from './zone.directive';

describe('ZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new MkZoneDirective();
    expect(directive).toBeTruthy();
  });
});
