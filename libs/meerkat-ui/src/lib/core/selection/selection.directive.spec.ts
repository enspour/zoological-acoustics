import { MkSelectionDirective } from './selection.directive';

describe('MkSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MkSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
