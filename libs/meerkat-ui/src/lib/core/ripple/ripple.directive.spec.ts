import { MkRippleDirective } from './ripple.directive';

describe('RippleDirective', () => {
  it('should create an instance', () => {
    const directive = new MkRippleDirective();
    expect(directive).toBeTruthy();
  });
});
