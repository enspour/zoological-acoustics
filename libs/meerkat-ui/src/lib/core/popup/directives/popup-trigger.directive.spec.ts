import { MkPopupTriggerDirective } from './popup-trigger.directive';

describe('MkPopupTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MkPopupTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
