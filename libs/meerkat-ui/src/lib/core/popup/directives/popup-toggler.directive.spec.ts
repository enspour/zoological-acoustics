import { MkPopupTogglerDirective } from './popup-toggler.directive';

describe('MkPopupTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new MkPopupTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
