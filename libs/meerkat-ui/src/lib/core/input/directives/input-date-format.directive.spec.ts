import { MkInputDateFormatDirective } from './input-date-format.directive';

describe('MkInputDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MkInputDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
