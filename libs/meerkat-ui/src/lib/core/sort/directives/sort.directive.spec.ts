import { MkSortDirective } from './sort.directive';

describe('MkSortDirective', () => {
  it('should create an instance', () => {
    const directive = new MkSortDirective();
    expect(directive).toBeTruthy();
  });
});
