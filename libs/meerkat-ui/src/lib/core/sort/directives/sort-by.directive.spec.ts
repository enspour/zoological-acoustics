import { MkSortByDirective } from './sort-by.directive';

describe('MkSortByDirective', () => {
  it('should create an instance', () => {
    const directive = new MkSortByDirective();
    expect(directive).toBeTruthy();
  });
});
