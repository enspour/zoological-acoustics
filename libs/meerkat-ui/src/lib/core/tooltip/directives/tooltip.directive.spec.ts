import { MkTooltipDirective } from './tooltip.directive';

describe('TooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new MkTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
