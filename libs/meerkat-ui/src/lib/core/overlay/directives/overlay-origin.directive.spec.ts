import { MkOverlayOriginDirective } from './overlay-origin.directive';

describe('OverlayOriginDirective', () => {
  it('should create an instance', () => {
    const directive = new MkOverlayOriginDirective();
    expect(directive).toBeTruthy();
  });
});
