import { MkTeleportDirective } from './teleport.directive';

describe('TeleportDirective', () => {
  it('should create an instance', () => {
    const directive = new MkTeleportDirective();
    expect(directive).toBeTruthy();
  });
});
