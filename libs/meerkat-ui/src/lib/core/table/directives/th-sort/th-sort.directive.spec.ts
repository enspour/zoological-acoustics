import { MkTableHeaderSortDirective } from './th-sort.directive';

describe('MkThSortDirective', () => {
  it('should create an instance', () => {
    const directive = new MkTableHeaderSortDirective();
    expect(directive).toBeTruthy();
  });
});
