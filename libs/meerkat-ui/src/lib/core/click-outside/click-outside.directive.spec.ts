import { MkClickOutsideDirective } from './click-outside.directive';

describe('MkClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new MkClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
