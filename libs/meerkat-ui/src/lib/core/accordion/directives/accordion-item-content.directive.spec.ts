import { MkAccordionItemContentDirective } from './accordion-item-content.directive';

describe('MkAccordionItemContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MkAccordionItemContentDirective();
    expect(directive).toBeTruthy();
  });
});
