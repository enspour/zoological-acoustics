import { MkSizeDirective } from './size.directive';

describe('MkSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MkSizeDirective();
    expect(directive).toBeTruthy();
  });
});
