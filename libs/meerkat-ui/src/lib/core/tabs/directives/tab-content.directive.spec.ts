import { MkTabContentDirective } from './tab-content.directive';

describe('TabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MkTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
