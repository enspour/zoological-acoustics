import { MkMenuTriggerDirective } from './menu-trigger.directive';

describe('MenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new MkMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
