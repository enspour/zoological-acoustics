import { MkVirtualizeByRangeDirective } from './virtualize-by-range.directive';

describe('MkVirtualizeByRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new MkVirtualizeByRangeDirective();
    expect(directive).toBeTruthy();
  });
});
