import { MkOptionsDirective } from './options.directive';

describe('OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new MkOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
