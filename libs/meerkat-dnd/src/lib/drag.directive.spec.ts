import { MkDndDragDirective } from './drag.directive';

describe('MkDndDragDirective', () => {
  it('should create an instance', () => {
    const directive = new MkDndDragDirective();
    expect(directive).toBeTruthy();
  });
});
