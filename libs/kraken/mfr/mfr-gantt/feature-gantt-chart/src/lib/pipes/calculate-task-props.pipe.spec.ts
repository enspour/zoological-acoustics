import { CalculateTaskPropsPipe } from './calculate-task-props.pipe';

describe('CalculateTaskPropsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTaskPropsPipe();
    expect(pipe).toBeTruthy();
  });
});
