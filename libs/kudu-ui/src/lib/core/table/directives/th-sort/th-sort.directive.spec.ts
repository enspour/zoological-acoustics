import { KuduTableHeaderSortDirective } from './th-sort.directive';

describe('KuduThSortDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduTableHeaderSortDirective();
    expect(directive).toBeTruthy();
  });
});
