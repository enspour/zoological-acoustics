import { KuduTableColumnDirective } from './column.directive';

describe('ColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduTableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
