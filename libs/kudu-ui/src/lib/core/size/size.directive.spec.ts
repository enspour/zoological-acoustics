import { KuduSizeDirective } from './size.directive';

describe('KuduSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduSizeDirective();
    expect(directive).toBeTruthy();
  });
});
