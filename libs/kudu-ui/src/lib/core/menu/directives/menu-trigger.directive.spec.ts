import { KuduMenuTriggerDirective } from './menu-trigger.directive';

describe('MenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
