import { InputDateValidatorDirective } from './input-date-validator.directive';

describe('InputDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
