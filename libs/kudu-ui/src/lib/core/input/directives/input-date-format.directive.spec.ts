import { KuduInputDateFormatDirective } from './input-date-format.directive';

describe('KuduInputDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduInputDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
