import { KuduOptionsDirective } from './options.directive';

describe('OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
