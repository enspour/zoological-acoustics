import { KuduSelectionDirective } from './selection.directive';

describe('KuduSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
