import { KuduVirtualizeByRangeDirective } from './virtualize-by-range.directive';

describe('KuduVirtualizeByRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduVirtualizeByRangeDirective();
    expect(directive).toBeTruthy();
  });
});
