import { KuduVirtualizationDirective } from './virtualization.directive';

describe('VirtualizationDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduVirtualizationDirective();
    expect(directive).toBeTruthy();
  });
});
