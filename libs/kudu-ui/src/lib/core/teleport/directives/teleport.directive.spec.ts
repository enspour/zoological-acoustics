import { KuduTeleportDirective } from './teleport.directive';

describe('TeleportDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduTeleportDirective();
    expect(directive).toBeTruthy();
  });
});
