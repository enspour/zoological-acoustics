import { KuduTabContentDirective } from './tab-content.directive';

describe('TabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduTabContentDirective();
    expect(directive).toBeTruthy();
  });
});
