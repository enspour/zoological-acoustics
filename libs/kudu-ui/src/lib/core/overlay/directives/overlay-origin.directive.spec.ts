import { KuduOverlayOriginDirective } from './overlay-origin.directive';

describe('OverlayOriginDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduOverlayOriginDirective();
    expect(directive).toBeTruthy();
  });
});
