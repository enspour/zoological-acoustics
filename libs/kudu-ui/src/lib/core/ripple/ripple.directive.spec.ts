import { KuduRippleDirective } from './ripple.directive';

describe('RippleDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduRippleDirective();
    expect(directive).toBeTruthy();
  });
});
