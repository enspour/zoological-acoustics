import { KuduSortDirective } from './sort.directive';

describe('KuduSortDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduSortDirective();
    expect(directive).toBeTruthy();
  });
});
