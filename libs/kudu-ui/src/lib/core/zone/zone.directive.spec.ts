import { KuduZoneDirective } from './zone.directive';

describe('ZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduZoneDirective();
    expect(directive).toBeTruthy();
  });
});
