import { KuduIntersectionDirective } from './intersection.directive';

describe('IntersectionDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduIntersectionDirective();
    expect(directive).toBeTruthy();
  });
});
