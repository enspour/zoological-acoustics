import { KuduAccordionItemContentDirective } from './accordion-item-content.directive';

describe('KuduAccordionItemContentDirective', () => {
  it('should create an instance', () => {
    const directive = new KuduAccordionItemContentDirective();
    expect(directive).toBeTruthy();
  });
});
