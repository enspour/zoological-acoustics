import { KanbanBoardSaverDirective } from './kanban-board-saver.directive';

describe('KanbanBoardSaverDirective', () => {
  it('should create an instance', () => {
    const directive = new KanbanBoardSaverDirective();
    expect(directive).toBeTruthy();
  });
});
