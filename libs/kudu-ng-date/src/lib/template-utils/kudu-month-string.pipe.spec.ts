import { KuduMonthStringPipe } from './kudu-month-string.pipe';

describe('KuduMonthStringPipe', () => {
  it('create an instance', () => {
    const pipe = new KuduMonthStringPipe();
    expect(pipe).toBeTruthy();
  });
});
