import { GetTypeAliasPipe } from './get-type-alias.pipe';

describe('GetTypeAliasPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTypeAliasPipe();
    expect(pipe).toBeTruthy();
  });
});
