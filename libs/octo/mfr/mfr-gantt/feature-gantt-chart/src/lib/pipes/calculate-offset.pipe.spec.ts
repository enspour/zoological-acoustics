import { CalculateOffsetPipe } from './calculate-offset.pipe';

describe('CalculateTaskOffsetPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateOffsetPipe();
    expect(pipe).toBeTruthy();
  });
});
